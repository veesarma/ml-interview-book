"""mlbook: from-scratch, interview-grade reference implementations.

Every module is written to be *read*, not just run:

* explicit shapes in comments on every tensor operation,
* no fused / clever tensors (Q, K, V are three separate projections),
* small functions with docstrings that state input and output shapes,
* NumPy for the mathematical foundations, PyTorch where autograd or GPUs matter.
"""

__version__ = "0.1.0"
