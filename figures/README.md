# Figure scripts

Each script writes one or more PNGs into `docs/assets/figures/` and is named
`<part>_<name>.py` → `<part>_<name>.png`. Run all of them with `make figures`.
Conventions: dpi 150, tight bounding box, white background, default tab10 palette.
